import { USERS_ENDPOINT } from "../config.js";
import { User, UserType } from "../models/User.js"; // schema User MongoDB
import type { Request, Response } from "express";

export const triggerVisit = async (req: Request, resp: Response) => {};
export const getMeInformation = async (req: Request, resp: Response) => {};
